# Dependencies to build the documentation site (MkDocs + Material), used by
# Read the Docs. These are independent of the application's own
# requirements.txt at the repo root.
mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-include-markdown-plugin>=6.0
